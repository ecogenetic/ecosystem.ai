# Vercel: set this in the project Environment Variables (Production + Preview).
# Required for Nextra "Last updated on" to use real git dates instead of a
# stale date from a shallow clone.
VERCEL_DEEP_CLONE=true
